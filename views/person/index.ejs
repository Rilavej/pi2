<%- include('../layouts/header'); -%>
    <section class="section">
        <div class="container">
            <h1 class="title">
                Aqui voce deve estar autenticado
            </h1>
            <% if (user) { %>
                <h1 class="title">
                    <%= user.name %>
                </h1>
                <p class="subtitle">
                    <%= JSON.stringify(user, null, null) %>
                </p>
                <h1 class="title">
                    <%= JSON.stringify(person, null, null) %>
                </h1>
            <% }  %>
        </div>
    </section>
    <%- include('../layouts/footer'); -%>
<%- include('../layouts/header'); -%>
    <section class="section">
        <div class="container">
            <div class="container">
                <% if (message) { %>
                    <p class="subtitle">
                        <strong>
                            <%= message %>
                        </strong>
                    </p>
                    <% } %>
            </div>
        </div>
    </section>
    <%- include('../layouts/footer'); -%>
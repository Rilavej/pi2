<section class="section">
    <div class="container is-max-desktop">
        <h1 class="subtitle is-4 has-text-centered">Encontre um profissional</h1>
        <form id="finderForm" action="/" method="post" autocomplete="off">
            <div class="columns">
                <div class="column field py-1">
                    <div class="control">
                        <select id="stateInput" class="input is-white" name="state" required >
                            <% if (inputValues) { %>
                                <option value="<%= inputValues.state %>" selected>
                                    <%= inputValues.state %>
                                </option>
                                <% } else { %>
                                    <option value="" disabled selected> Estado </option>
                                    <% } %>
                                        <% ufs.forEach(uf=> { %>
                                            <option value=<%=uf.abbreviation %>><%= uf.name %>
                                            </option>
                                            <% }); %>
                        </select>
                    </div>
                </div>
                <div class="column field py-1">
                    <div id="autocompleteWrapperCities" class="control">
                        <% if (inputValues) { %>
                            <input id="cityInput" class="input is-white" name="city" type="text" required
                                autocomplete="off" value="<%= inputValues.city %>">
                            <% } else { %>
                                <input id="cityInput" class="input is-white" name="city" type="text" required disabled
                                    placeholder="Cidade" autocomplete="off">
                                <% } %>

                    </div>
                </div>
                <div class="column field py-1">
                    <div class="control autocompleteWrapperCbo">
                        <% if (inputValues) { %>
                            <input class="input is-white cboInput" name="service" type="text" required
                                autocomplete="off" value="<%= inputValues.service %>">
                            <% } else { %>
                                <input class="input is-white cboInput" name="service" type="text" required
                                    placeholder="profissional" autocomplete="off">
                                <% } %>

                    </div>
                </div>
                <div class="column field py-1">
                    <div class="control">
                        <button id="searchButton" class="button is-fullwidth is-link ">Pesquisar</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</section>
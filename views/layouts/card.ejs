<div class="container">
    <% if (user) { %>
        <h1 class="title">
            <%= user.name %>
                <br>
                <%= JSON.stringify(user, null, null) %>
        </h1>
        <p class="subtitle">
            <%= JSON.stringify(person, null, null) %>
        </p>
        <% } %>
</div>
